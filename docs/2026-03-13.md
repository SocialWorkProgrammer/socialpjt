# 2026-03-13 진행 기록

## 작업 요약
- 3종 공공데이터 API 스펙 수집 및 필드 매핑 방향 확정(중앙부처복지서비스, 지자체복지서비스, 복지서비스정보).
- 환경 변수 운용 확정: `SOCIAL_SERVICE_API_KEY` 사용, `PUBLIC_API_KEY`는 빈 문자열 유지.
- 수집 정책 확정: DB에 저장 후 하루 1회 갱신.
- 코드표 처리 확정: 중앙부처는 코드 저장 + 코드표 한글명 주석, 지자체는 한글명 필드 저장.
- 복지서비스정보 한글 컬럼은 영문 필드로 매핑하고 코드 주석 + 문서 매핑표로 한글 설명 병기.

## 중앙부처복지서비스 (NationalWelfareInformationsV001)
- 목록 조회 응답 주요 필드
  - `servId`, `servNm`, `servDgst`, `servDtlLink`, `jurMnofNm`, `jurOrgNm`,
    `lifeArray`, `trgterIndvdlArray`, `intrsThemaArray`, `onapPsbltYn`,
    `sprtCycNm`, `srvPvsnNm`, `svcfrstRegTs`, `inqNum`
- 상세 조회 응답 주요 필드
  - `tgtrDtlCn`, `slctCritCn`, `alwServCn`, `crtrYr`, `wlfareInfoOutlCn`, `rprsCtadr`
  - 리스트: `applmetList`, `inqplCtadrList`, `inqplHmpgReldList`, `basfrmList`, `baslawList`
- 코드표 처리
  - `lifeArray`, `trgterIndvdlArray`, `intrsThemaArray`는 코드 그대로 저장
  - 코드표 한글명은 주석으로 관리

## 지자체복지서비스 (LocalGovernmentWelfareInformations)
- 목록 조회 응답 주요 필드
  - `servId`, `servNm`, `servDgst`, `servDtlLink`, `sprtCycNm`, `srvPvsnNm`,
    `aplyMtdNm`, `inqNum`, `lastModYmd`, `bizChrDeptNm`, `ctpvNm`, `sggNm`,
    `lifeNmArray`, `trgterIndvdlNmArray`, `intrsThemaNmArray`
- 상세 조회 응답 주요 필드
  - `enfcBgngYmd`, `enfcEndYmd`, `sprtTrgtCn`, `slctCritCn`, `alwServCn`, `aplyMtdCn`
  - 리스트: `inqplCtadrList`, `inqplHmpgReldList`, `baslawList`, `basfrmList`
- 코드표 처리
  - 요청 파라미터는 코드 사용, 응답은 한글명(`NmArray`) 저장

## 복지서비스정보 (odcloud)
- base_url: `https://api.odcloud.kr/api/15083323/v1/uddi:3929b807-3420-44d7-a851-cc741fce65a1`
- 응답 주요 필드
  - `서비스아이디`, `서비스명`, `서비스URL`, `서비스요약`, `사이트`, `대표문의`,
    `소관부처명`, `소관조직명`, `기준연도`, `최종수정일`
- 필드 매핑 원칙
  - 한글 컬럼을 영문 필드로 매핑하고, 코드 주석 + 문서 매핑표에 한글 설명 병기

## 모델/수집 설계 방향(초안)
- 공통 필드: `external_id`, `title`, `summary`, `detail_url`, `site_url`,
  `ministry`, `organization`, `base_year`, `last_modified`, `source`
- 상세 텍스트: `target_detail`, `selection_criteria`, `benefit_detail`, `apply_method`
- 지역: `region_ctpv`, `region_sgg`
- 리스트(JSON): `contact_list`, `homepage_list`, `law_list`, `form_list`
- 중복 방지: `external_id + source` 기준 `update_or_create`
- 수집 흐름: 목록 조회 후 상세 보강(중앙부처/지자체), 복지서비스정보는 목록만 저장

## 다음 작업
- 3종 API 통합 매핑표 작성(영문 필드 + 한글 주석 병기).
- `SocialService` 모델 최종 확정 및 수집 커맨드 설계.
- 게시판 검색/필터 설계(코드/한글명 혼재 처리).
- `.env` 및 cron 문서 갱신.
